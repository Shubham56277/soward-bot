name: Deploy Bot to AWS (Legacy)
# This workflow is superseded by deploy-production.yml
# Kept for reference but disabled.

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Use deploy-production.yml instead"
